header.weekdays
    each day in ['Sun', 'Mon', 'Tues', 'Wed', 'Thu', 'Fri', 'Sat']
        div.weekday= day
section.days
    each week in days
        section.week
            each day in week
                article.day(data-date=day.date)
                    h2.date= day.day
                    section.events
